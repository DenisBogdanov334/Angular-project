import { StartsWithEPipe } from './starts-with-e.pipe';

describe('StartsWithEPipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithEPipe();
    expect(pipe).toBeTruthy();
  });
});
