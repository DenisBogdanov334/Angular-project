import { StartsWithTPipe } from './starts-with-t.pipe';

describe('StartsWithTPipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithTPipe();
    expect(pipe).toBeTruthy();
  });
});
